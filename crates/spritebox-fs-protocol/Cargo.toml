[package]
name = "spritebox-fs-protocol"
version = "0.1.0"
edition = "2024"
description = "Wire protocol types for the spritebox virtual filesystem"
license = "MIT"

[dependencies]
serde = { workspace = true }
bytes = { workspace = true }

[dev-dependencies]
serde_json = "1"
